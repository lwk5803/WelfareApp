# 개발 일지 (비전공자용 설명 포함)

이 파일은 코드가 "왜" 이렇게 바뀌었는지, 어떤 개념을 썼는지 비전공자도 이해할 수 있게 기록하는 곳입니다.
노트북/데스크톱 두 환경에서 git으로 동기화되므로, 어느 컴퓨터에서 작업하든 이 파일을 먼저 읽으면 흐름을 따라잡을 수 있습니다.

형식: 날짜 / 무엇을 했는지 / 왜 했는지(배경) / 참고 개념.

---

## 2026-08-13 — 1·2차 외부 테스트 피드백 정리 및 작업 계획

**무엇을**: 코드 변경은 아직 없음. 1차/2차 외부 테스트(ngrok으로 배포) 피드백 8건을 분석하고 우선순위/작업 방식을 정리함.

**피드백 원문 요약**:
1. 회원 등록/수정/삭제를 별도 메뉴로 분리
2. 실배포 시 로그인 및 권한 관리(등록은 누구나, 삭제는 등록자/담당자만)
3. 회원 등록 정보 세분화 (조손가정, 독거노인, 노인부부, 장애여부 등)
4. 위 세분화된 정보를 AI 추천(`ai_recommend.py`)에 반영해 더 구체적인 추천
5. 전반적 UI를 사용자 친화적으로 개선
6. 한 메뉴에서 작업 중 다른 메뉴로 이동하면 실행 중이던 작업이 멈춤 (병렬 처리 필요)
7. 주소 검색이 구주소/도로명이 정확하지 않으면 실패, 건물명 검색도 안 됨
8. 사진 촬영 기능 추가 (모바일/태블릿에서 촬영→등록→서류 자동 생성/출력)

**분류 및 우선순위**: 아래 "작업 계획" 섹션 참고. 큰 결정이 필요한 항목(로그인/권한, 병렬처리 구조, 사진+문서화, UI 전면 개편)은 별도 세션으로 나눠서 진행하기로 함.

**참고 개념**: (다음 실제 코드 변경부터 여기 추가 예정)

---

## 2026-08-13 — #1 회원 등록/수정/삭제 메뉴 분리

**무엇을**: `app_frontend.py`의 사이드바 메뉴를 `["회원 목록", "회원 등록/수정/삭제", "회원 통계", "추천 복지 서비스"]`에서
`["회원 목록", "신규 회원 등록", "회원 수정", "회원 삭제", "엑셀 일괄등록", "회원 통계", "추천 복지 서비스"]`로 분리함.
기존엔 "회원 등록/수정/삭제" 메뉴 하나 안에 탭(`st.tabs`) 3개(신규등록/수정삭제/엑셀등록)로 묶여 있었는데, 각각 최상위 메뉴 항목으로 뺐고, 수정과 삭제도 서로 다른 메뉴로 나눔.

**왜**: 1·2차 외부 테스트 피드백 — "회원 등록/수정/삭제도 다른 메뉴들처럼 따로 보였으면 좋겠다, 관리하기 편할 것 같다."

**참고 개념**:
- Streamlit의 `st.sidebar.radio`는 사용자가 고른 값을 파이썬 변수로 그대로 돌려주는 라디오 버튼입니다. `if/elif` 사슬로 "지금 메뉴 값이 뭐냐"에 따라 화면을 다르게 그리는 구조라, 메뉴를 늘리는 건 그냥 `elif` 블록 하나를 추가하는 것과 같습니다.
- `st.tabs`(탭)와 사이드바 메뉴는 둘 다 "화면 전환" 수단이지만, 탭은 한 메뉴 안에 소속된 하위 화면이라는 뜻이고, 사이드바 메뉴는 완전히 독립된 화면이라는 뜻이라 이번처럼 "따로 관리하고 싶다"는 요구엔 사이드바 메뉴가 더 맞습니다.

---

## 2026-08-13 — #7 주소 검색: 건물명/상호명 검색 안 되던 문제 수정

**무엇을**: `address_api.py`의 `search_road_address()`가 카카오 "주소 검색 API"로 결과가 없을 때, "키워드 검색 API"로 한 번 더 시도하도록 fallback을 추가함. 결과 항목에 `place_name`(건물명/상호명)을 추가해서, `app_frontend.py`의 주소 선택 드롭다운에 건물명이 있으면 같이 보여주도록 함(`main.py`는 두 파일을 그대로 연결만 하고 있어서 변경 없음).

**왜**: 피드백 #7 — "주소 검색 시 구주소/도로명이 정확하지 않으면 검색 실패, 건물명으로 검색해도 빈 내용."
원인은 카카오의 "주소 검색 API"(`/v2/local/search/address.json`)가 정확한 지번·도로명 형식만 인식하고, 건물명·상호명(예: "롯데월드타워", "OO경로당")은 애초에 이 API가 다루는 범위가 아니었기 때문. 카카오 "키워드 검색 API"(`/v2/local/search/keyword.json`)를 fallback으로 붙여서 해결.

**검증**: `curl`로 "롯데월드타워" 검색 → 이전엔 빈 배열, 수정 후엔 도로명주소+건물명이 정상 반환됨. 기존 정확한 도로명 검색("서울 강남구 테헤란로 123")도 우편번호 포함해서 그대로 잘 동작하는 것 확인(회귀 없음).

**참고 개념**:
- 외부 API를 쓸 때는 "이 API가 정확히 뭘 검색해주는 API인가"를 문서에서 확인하는 게 중요합니다. 카카오는 같은 "주소 찾기"처럼 보여도 목적이 다른 API를 여러 개 제공합니다 — 정확한 주소 문자열 매칭용(주소 검색), 장소/건물/상호명 매칭용(키워드 검색). 하나로 안 되면 다른 API로 다시 시도하는 방식을 "fallback(대체 시도)"이라고 부릅니다.
- 우편번호(zip_code)는 "주소 검색 API" 결과에만 들어있고 "키워드 검색 API"엔 없어서, 건물명으로 찾은 주소는 우편번호가 빈 채로 나옵니다. 필요하면 나중에 우편번호만 별도 조회하는 API를 추가로 붙일 수 있습니다.

---

## 2026-08-13 — #3/#4 회원 등록 정보 세분화 + AI 추천 연계

**무엇을**:
1. `sql/002_add_household_disability.sql` 추가 — `clients` 테이블에 `household_types`(가구 유형, 콤마구분 다중값), `has_disability`(예/아니오), `disability_type`(자유텍스트) 컬럼 신설. **⚠️ 이 SQL은 Supabase SQL Editor에서 직접 실행해야 적용됩니다 — 앱 코드만으로는 테이블 구조를 못 바꿔요.**
2. `main.py`(`ClientCreate`/`ClientUpdate`), `supabase_db.py`(`add_client`/`update_client`/`CLIENT_COLUMNS`), `app_frontend.py`(신규등록/수정 화면)에 새 필드 3개 반영. 가구 유형은 `st.multiselect`로 복수 선택(독거노인/노인부부/조손가정/한부모가정/다문화가정/장애인가구/일반가구), 장애 여부는 예/아니오 라디오 + 있을 때만 보이는 유형 텍스트칸.
3. `gov_welfare_api.py`의 `split_general_and_special()`에 `known_statuses` 매개변수 추가. 기존엔 "장애인" 자격이 필요한 복지서비스를 발견해도 회원이 실제 장애인인지 알 수 없어서 무조건 "참고(구역B)"로만 보여줬는데, 이제 등록 정보에 `has_disability=예`가 있으면 그 서비스를 바로 "일반 후보(구역A)"로 승격시킵니다. 다문화가정도 동일한 방식으로 처리.
4. `main.py`의 `profile_summary`(AI에게 넘기는 회원 프로필 텍스트)에 "가구 유형", "장애 여부" 줄 추가. 공공데이터 검색 키워드(`note_keyword`)도 가구 유형이 있으면 그걸 우선 사용하도록 변경(기존엔 "비고" 텍스트만 봤음).

**왜**: 피드백 #3("정보가 더 세분화되면 좋겠다") + #4("세분화되면 AI 추천이 더 구체적일 것"). 실제로 조손가정/독거노인 같은 정보가 없으면 AI가 일반적인 추천만 하게 되는데, 이 정보가 검색 키워드와 프로필에 들어가면 관련성 높은 서비스를 더 잘 찾아줍니다.

**참고 개념**:
- DB 컬럼 추가 같은 "스키마 변경"(SQL의 `ALTER TABLE`)은 이 앱이 쓰는 Supabase API 키(`SUPABASE_SECRET_KEY`)로는 할 수 없습니다. 이 키는 "데이터를 읽고 쓰는" 권한(REST API, PostgREST라는 방식)만 주고, "테이블 구조 자체를 바꾸는" 권한은 Supabase 웹 콘솔의 SQL Editor에서만 실행할 수 있어요. 그래서 SQL 파일을 저장소에 만들어두고, 실행은 사람이 콘솔에서 직접 하는 방식으로 나눴습니다.
- `known_statuses`처럼 "이미 확정된 사실을 코드에 미리 알려주는" 방식을 쓰면, AI(LLM)가 매번 추측하지 않아도 되어 결과가 더 정확하고 일관됩니다. AI에게 모든 판단을 맡기기보다, 확실한 규칙은 코드에서 처리하고 AI는 애매한 부분만 판단하게 나누는 게 좋은 설계입니다.

**⚠️ 다음 작업(노트북 포함) 전 필수**: Supabase 대시보드 → SQL Editor에서 `sql/002_add_household_disability.sql` 내용을 실행해야 신규 등록/수정이 정상 동작합니다. 실행 전에는 회원 등록 시 "column household_types does not exist" 같은 오류가 날 수 있습니다.

**후속 확인 (2026-08-13, 같은 세션)**: 사용자가 Supabase SQL Editor에서 마이그레이션 실행 완료. `/api/clients` POST로 `household_types="독거노인,장애인가구"`, `has_disability="예"`, `disability_type="지체장애"` 값을 넣어 테스트 등록 → 정상 저장 확인 후 테스트 데이터 삭제함. 이 세션의 #1/#3/#4/#7 항목은 모두 코드+실제 API 동작까지 검증 완료.

---

## 2026-08-13 — #2 로그인/권한 관리 추가

**무엇을**:
1. `sql/003_add_profiles_and_auth.sql` 추가 — `profiles` 테이블(사용자 id + role) 신설, `auth.users`에 새 계정이 생기면 자동으로 `profiles`에도 기본 role(`staff`)로 등록되는 트리거 포함. **⚠️ 이것도 Supabase SQL Editor에서 직접 실행 필요.**
2. `auth.py` 신규 — Supabase Auth로 로그인(`login()`)하고, FastAPI가 요청마다 로그인 토큰(JWT)이 진짜인지 검증하는 `require_user`/`require_admin` 두 개의 "문지기" 함수를 만듦.
3. `main.py` — `/api/auth/login` 엔드포인트 추가. 회원 등록(`POST`)/수정(`PUT`)은 로그인한 사람이면 누구나, 삭제(`DELETE`)는 `role=admin`인 사람만 되도록 각 엔드포인트에 `Depends(auth.require_user)` / `Depends(auth.require_admin)`을 붙임.
4. `app_frontend.py` — 앱 진입 시 로그인 화면을 먼저 보여주고, 로그인 성공하면 이메일/비밀번호로 받은 토큰을 이후 모든 등록/수정/삭제 요청에 실어 보냄. "회원 삭제" 메뉴 자체도 관리자가 아니면 사이드바에 안 보이게 숨김(화면에서도 안 보이고, 혹시 API를 직접 두드려도 서버가 한 번 더 막음 — "이중 방어").

**왜**: 피드백 #2 — "실배포 시 로그인 관리로 아무나 로그인 못하게, 삭제는 등록자/담당자만." 두 단계 역할(직원=등록·수정 가능/삭제 불가, 관리자=전부 가능)로 결정함(사용자 확인 완료).

**검증**: 잘못된 이메일/비밀번호로 로그인 시도 → 정상적으로 거부됨. 토큰 없이 회원 등록/삭제 API를 직접 호출 → 401(로그인 필요)로 차단됨. 실제 로그인 성공 흐름은 Supabase에 계정이 있어야 테스트 가능해서, 사용자가 SQL 실행 + 계정 생성 후 확인 예정.

**참고 개념**:
- **JWT(JSON Web Token)**: 로그인에 성공하면 서버가 "이 사람이 로그인했다"는 걸 증명하는 암호화된 문자열(토큰)을 하나 줍니다. 이후 요청마다 이 토큰을 같이 보내면, 서버는 매번 비밀번호를 다시 묻지 않고 토큰만 보고 "누구인지, 무슨 역할인지" 확인할 수 있습니다. 자동차 발렛 티켓과 비슷합니다 — 티켓(토큰)만 보여주면 다시 신분증(비밀번호) 안 보여줘도 되는 것.
- **JWKS(공개키 목록)**: 우리 서버가 "이 토큰이 진짜 Supabase가 만든 게 맞나"를 확인할 때 쓰는 열쇠 꾸러미입니다. Supabase가 공개적으로 제공하는 검증용 열쇠라서, 우리 서버는 비밀번호 없이도 토큰의 진위만 확인할 수 있습니다.
- **이중 방어(defense in depth)**: 화면(프론트엔드)에서 "삭제" 버튼을 숨기는 것만으로는 진짜 보안이 안 됩니다(개발자도구로 우회 가능). 그래서 서버(백엔드) 쪽에서도 한 번 더 권한을 확인하도록 만들었습니다 — 화면은 사용자 편의용, 서버 검사는 실제 보안용.
- **st.session_state**: Streamlit에서 로그인 정보처럼 "이 화면 저 화면 옮겨다녀도 기억해야 하는 값"을 담아두는 임시 저장소입니다. 브라우저 탭을 새로고침하면 초기화되니, 지금 방식은 완전한 "로그인 유지"는 아니고 세션(탭 하나) 단위 로그인입니다 — 나중에 필요하면 쿠키 기반으로 개선할 수 있습니다.

**⚠️ 다음 작업(노트북 포함) 전 필수**:
1. Supabase SQL Editor에서 `sql/003_add_profiles_and_auth.sql` 실행
2. Supabase 대시보드 → Authentication → Users → "Add user"로 계정 생성 (이메일/비밀번호 직접 입력)
3. 관리자로 만들 계정은 SQL Editor에서: `update profiles set role = 'admin' where email = '해당 이메일';`

---

## 2026-08-13 — #2 관련 버그 수정: 공유 Supabase 클라이언트가 요청 중간에 고장나던 문제

**무엇을**: `supabase_db.py`의 `_supabase()`에서 `@lru_cache`를 제거함. 이제 Supabase 클라이언트를 요청마다 새로 만듭니다(약간의 성능 손해는 있지만, 직원 몇 명이 쓰는 앱 규모에서는 무시할 수준).

**왜**: 로그인 기능을 붙이고 나서 실제로 등록을 시도했더니 "회원 등록 중 오류" 400 에러가 났고, 회원 목록도 갑자기 빈 배열만 나오는 증상을 사용자가 보고함. 재현/추적 결과:
- 원래 `_supabase()`는 `@lru_cache`로 서버가 켜져 있는 동안 클라이언트 인스턴스 하나를 계속 재사용하고 있었음(로그인 기능 추가 전부터 있던 코드).
- FastAPI는 `def`(동기 함수)로 만든 엔드포인트를 요청마다 별도 스레드에서 실행합니다. 로그인 기능을 추가하면서 거의 모든 요청(등록/수정/삭제뿐 아니라 권한 확인까지)이 이 하나의 공유 클라이언트를 동시에 여러 스레드에서 두드리게 됐고, 어느 시점부터 그 클라이언트의 내부 연결 상태가 꼬여서 조회는 빈 배열을, 등록/권한조회는 알 수 없는 오류를 돌려주기 시작함.
- 서버 프로세스를 완전히 재시작하면 일시적으로 정상화됐다가, 요청이 좀 쌓이면 다시 같은 증상이 나타나는 패턴으로 확인 — "재시작하면 낫는다"는 전형적인 상태 손상(state corruption) 버그의 특징.
- `@lru_cache` 제거 후 반복 테스트(연속 등록 3건, 목록 조회 8회, 관리자 삭제 6건)로 재발하지 않는 것 확인.

**참고 개념**:
- **연결 재사용(커넥션 풀) vs 매번 새로 연결**: 보통은 네트워크 연결을 재사용하는 게 더 빠르지만, 그 재사용 로직이 "여러 요청이 동시에 같은 연결을 건드려도 안전하다(thread-safe)"는 보장이 없으면 오히려 위험합니다. 이번 경우가 그런 경우였어요 — 안전성이 확인 안 된 재사용보다는, 조금 느려도 매번 새로 만드는 쪽이 더 신뢰할 수 있었습니다.
- 이런 "가끔 재시작해야 낫는" 버그는 보통 어딘가에 "여러 스레드/요청이 공유하는 상태"가 있고, 그게 서로 충돌할 때 생깁니다. 재시작하면 그 꼬인 상태가 초기화돼서 일시적으로 괜찮아 보이는 게 오히려 단서가 됩니다.

---

## 2026-08-14 — 목록 연번 표시 + 영구 회원번호(YYYY-0001) 추가

**무엇을**:
1. `sql/004_add_member_no.sql` 추가 — `clients` 테이블에 `member_no` 컬럼 신설. 등록 시 DB 트리거가 자동으로 "등록연도-4자리일련번호"(예: `2026-0001`)를 채워줌. 기존 회원들도 등록일시 순서로 소급 부여됨. **⚠️ Supabase SQL Editor 실행 완료됨(2026-08-14).**
2. `supabase_db.py`의 `CLIENT_COLUMNS`에 `member_no` 추가 (별도 app 코드에서 값을 만들 필요 없음 — DB 트리거가 처리).
3. `app_frontend.py`: 회원 목록 화면에 "연번"(1, 2, 3...) 컬럼 추가 — DB에 저장되는 값이 아니라 화면에 표시할 때마다 새로 매기는 순번이라, 회원이 삭제돼도 항상 빈틈없이 1번부터 보임. 내부 `id` 컬럼은 목록 화면에서 숨김(직원이 볼 필요 없는 값). 회원 수정/삭제 화면의 "회원 선택" 드롭다운도 내부 id 대신 `member_no`를 보여주도록 변경.

**왜**: 사용자가 반복 테스트로 회원 번호가 듬성듬성해 보이는 걸 보고 "삭제 시 번호를 당겨서 재정렬해달라"고 요청. 논의 끝에 두 개념을 분리하기로 함:
- **연번(화면 표시용)**: 항상 빈틈없이 보여야 함 → DB에 저장 안 하고 화면에서 매번 계산.
- **회원번호(영구 식별자, #8 서류 출력 기능과 연계)**: 한번 부여되면 절대 안 바뀌어야 함(삭제된 회원 자리를 당겨서 재사용하면, 이미 나간 서류/기록의 번호와 어긋나 버림) → DB에 저장하고 재사용 안 함. 사용자가 이 정책에 동의함.

**검증**: 실제 3명 등록 → `2026-0001/0002/0003` 확인. 가운데(`0002`) 삭제 후 재등록 → 새 회원은 `0002`를 재사용하지 않고 `0004`를 받는 것 확인(의도한 동작). 테스트 데이터는 모두 삭제해서 정리.

**참고 개념**:
- **DB 트리거**: "이 테이블에 무슨 일이 생기면(예: 새 행이 insert되면) DB가 자동으로 실행하는 코드"입니다. 회원번호를 앱 코드(Python)에서 계산하지 않고 트리거로 처리한 이유는, 나중에 노트북에서든 다른 프로그램에서든 이 테이블에 데이터를 넣기만 하면 항상 똑같은 규칙으로 번호가 매겨지게 하기 위해서입니다 — 규칙이 앱 코드가 아니라 데이터베이스 자체에 있는 셈입니다.
- **표시용 값 vs 저장되는 값**: "연번"처럼 화면에 보여줄 때만 계산하고 저장은 안 하는 값과, `member_no`처럼 한 번 정해지면 계속 유지되어야 하는 값을 구분하는 게 이번 설계의 핵심입니다. 저장이 필요 없는 걸 굳이 저장하면 나중에 값이 꼬일 여지만 늘어납니다.

---

## 2026-08-14 — 로그인 유지(쿠키) + 소프트 삭제 + 돌아온 회원 이력 확인 + 더미데이터

**무엇을**:
1. **로그인 유지**: `extra-streamlit-components` 라이브러리로 브라우저 쿠키에 `refresh_token`을 저장. 새로고침(F5)하거나 브라우저를 껐다 켜도(최대 30일) 자동으로 로그인이 이어짐. `auth.py`에 `refresh()` 함수, `main.py`에 `/api/auth/refresh` 엔드포인트 추가.
2. **소프트 삭제**: `sql/005_soft_delete.sql`로 `clients`에 `deleted_at` 컬럼 추가. `delete_client()`가 이제 행을 진짜로 지우지 않고 `deleted_at`만 채움. `get_all_clients()`/`get_client()`는 `deleted_at is null`인 회원만 돌려주므로, 목록/통계/추천 등 평소 화면에는 삭제된 회원이 안 보임(동작은 이전과 동일하게 느껴짐).
3. **돌아온 회원 이력 확인**: 신규 회원 등록 화면 맨 위에 "🔎 기존 등록 이력 확인" 영역 추가. 이름+생년월일로 조회하면, 기존에 있던 `check_duplicates` API(원래 코드엔 있었지만 화면에서 한 번도 안 쓰이고 있었음)를 이용해 (a) 이미 등록된 활성 회원이면 경고, (b) 예전에 탈퇴 처리된 기록이 있으면 "이 정보 불러오기" 버튼으로 이름/주소/가구유형/장애정보 등을 새 등록 폼에 자동으로 채워줌. 실제로 새로 만들어지는 건 새 행(새 회원번호)이고, 예전 행이 되살아나는 게 아님 - 정보만 재사용.
4. **더미데이터**: 실제 케이스를 흉내낸 회원 5명(독거노인, 노인부부+장애, 조손가정, 한부모+다문화, 장애인가구) 등록.

**왜**: 사용자 피드백 3건 — (1) 새로고침마다 재로그인해야 하는 번거로움, (2) 화면 확인용 데이터 필요, (3) "예전에 다니다 그만둔 회원이 다시 등록하러 오면 예전 정보를 재사용하고 싶다"는 실제 현장 시나리오. (3)은 논의 끝에 "번호는 새로 받고 정보만 재사용" 정책으로 확정.

**검증**: `curl`로 등록→소프트삭제→목록에서 사라짐 확인→이력확인 API로 `deleted_at` 있는 상태로 잡히는 것 확인. 로그인 refresh 엔드포인트도 curl로 새 access_token/refresh_token 발급되는 것 확인. 프론트 쿠키 저장/자동로그인은 브라우저 동작이라 사용자 쪽 확인 필요.

**참고 개념**:
- **소프트 삭제(soft delete)**: "지웠다"고 표시만 하고 실제 데이터는 남겨두는 방식입니다. 진짜로 지우면(hard delete) 그 기록은 영원히 사라져서, 이번처럼 "예전 회원이 다시 왔을 때 알아보기" 같은 기능 자체가 불가능해집니다. 대신 평소 화면에서는 "지운 것처럼" 안 보이게 필터링만 해주면 됩니다.
- **쿠키(cookie)**: 브라우저에 아주 작은 정보를 저장해뒀다가, 같은 사이트에 다시 접속할 때 서버가 다시 읽을 수 있게 하는 표준 기술입니다. 여기서는 "이 사람이 로그인했었다"는 증표(refresh_token)를 쿠키에 담아뒀다가, 페이지를 새로 열 때 그 증표로 자동 로그인을 시도합니다.
- **refresh_token vs access_token**: `access_token`은 유효기간이 짧고(1시간) API 요청마다 실제로 쓰이는 "출입증"입니다. `refresh_token`은 유효기간이 훨씬 길고, "출입증을 새로 발급받을 수 있는 자격"만 증명합니다. 출입증(access_token)을 오래 유지되는 곳(쿠키)에 두면 탈취됐을 때 위험이 크므로, 오래 두는 건 refresh_token만이고 access_token은 매번 새로 받습니다.

**후속 버그 수정 (2026-08-14)**: 로그인 유지가 실제로는 동작 안 하는 문제가 2건 있었음, 둘 다 수정 후 사용자가 브라우저에서 정상 동작 확인:
1. `st.cache_resource`로 `CookieManager`를 감쌌더니 "CachedWidgetWarning" 에러 발생 — CookieManager는 내부적으로 위젯처럼 동작해서 캐시 데코레이터 안에 두면 안 됨. 캐싱 제거하고 매 실행마다 새로 만들되 `key`를 고정해서 해결.
2. 쿠키를 브라우저에 저장(`cookie_manager.set()`)한 직후 바로 `st.rerun()`을 불러서, 브라우저가 실제로 쿠키를 저장할 시간도 없이 화면이 바뀌어버리는 경쟁 상태(race condition)가 있었음. 로그인 성공 시 `st.rerun()`을 강제로 부르지 않고 스크립트가 자연스럽게 끝까지 이어지도록(같은 실행 안에서 로그인 화면 대신 바로 메인 화면까지 그려지도록) 구조를 바꿔서 해결. 이제 새로고침하면 로그인 화면이 아주 잠깐 비쳤다가 자동으로 메인 화면으로 넘어가는 정도로 동작함(정상).

---

## 2026-08-14 — #6 AI 추천 조회를 백그라운드로 전환 (메뉴 이동해도 안 끊기게)

**무엇을**:
1. `main.py`의 `fetch_initial_recommendations` 엔드포인트 로직을 순수 함수 `_compute_initial_recommendations(client_id)`로 분리(FastAPI 라우팅과 분리해서, 스레드에서도 그냥 호출할 수 있게).
2. 메모리 안에 작업 상태를 담아두는 간단한 저장소(`_recommend_jobs` 딕셔너리)를 만들고, `POST /api/recommend/start`로 백그라운드 스레드에서 조회를 시작 → 즉시 `{"status":"running"}` 응답 → `GET /api/recommend/status/{client_id}`로 진행 상태(`running`/`done`/`error`/`none`) 확인하는 구조로 바꿈. 기존의 한 번에 다 처리하던 `/api/recommend/fetch_initial`은 제거.
3. `app_frontend.py`의 "추천 복지 서비스" 화면을 이 상태값에 맞춰 다시 그림: `running`이면 2초마다 자동으로 상태를 다시 확인(`time.sleep(2); st.rerun()`), `done`이면 결과를 채워서 보여줌. "대화 초기화" 버튼은 이제 화면만 지우는 게 아니라 서버에 새 조회를 다시 요청함.

**왜**: 피드백 #6 — "한 메뉴에서 기능을 수행하다가 다른 메뉴로 넘어가면 먼저 실행하던 기능이 정지되는 것 같다." 원인은 Streamlit의 동작 방식 자체(사용자가 뭔가 하면 파이썬 스크립트를 처음부터 다시 실행) 때문에, AI 추천처럼 몇 초~몇십 초 걸리는 작업을 화면(Streamlit) 쪽에서 기다리고 있으면 메뉴를 옮기는 순간 그 기다림 자체가 사라져 버렸던 것. 조회 작업 자체를 화면이 아니라 서버(FastAPI 백엔드)에서 돌아가는 별도 스레드로 옮겨서, 화면이 뭘 하든 서버 쪽 작업은 안 끊기게 만듦.

**검증**: `curl`로 `/start` 호출 직후 응답이 0.1초 안에 옴(작업이 논블로킹으로 시작됨) 확인. 그 사이 다른 API(`/api/clients`) 요청도 즉시 응답되는 것 확인(작업이 서버 전체를 막지 않음). 몇 초 후 `/status`로 폴링하니 `done` 상태와 함께 정상적인 추천 결과(장애인가구 회원에게 장애인자립자금대여 등이 참고가 아니라 바로 추천되는 것 포함)가 돌아오는 것 확인.

**참고 개념**:
- **동기(synchronous) vs 백그라운드(background) 실행**: "동기"는 요청한 사람이 결과가 나올 때까지 그 자리에서 계속 기다리는 방식이고, "백그라운드"는 일단 "시작했다"고만 알려주고 실제 작업은 뒤에서 따로 진행되는 방식입니다. 오래 걸리는 작업일수록 백그라운드로 돌리고 "다 됐는지"는 나중에 따로 물어보는(폴링, polling) 구조가 화면이 멈추거나 끊기는 걸 막아줍니다.
- **스레드(thread)**: 한 프로그램 안에서 여러 작업을 "동시에" 진행하는 단위입니다. 여기서는 AI 추천 조회를 별도 스레드에 맡겨서, 그 스레드가 일하는 동안에도 서버는 다른 사람들의 다른 요청(회원 목록 조회 등)을 계속 처리할 수 있습니다.
- **폴링(polling)**: "혹시 다 됐어요?"를 주기적으로 계속 물어보는 방식입니다. 여기서는 화면이 2초마다 서버에 "그 작업 다 됐어?"라고 물어보다가, "다 됐다"는 답이 오면 그때 결과를 화면에 채워 넣습니다.

---

## 2026-08-14 — #8 사진 촬영 + 자동 문서화

**무엇을**:
1. **사진 촬영**: 신규 회원 등록/회원 수정 화면에 `st.camera_input()`(브라우저 카메라, 태블릿/휴대폰에서도 바로 동작) 추가. 촬영한 사진은 800x800 이하로 리사이즈 + JPEG로 압축해서 base64 텍스트로 인코딩 후 DB에 저장(`clients.photo_data`, `sql/006_add_photo.sql`). 별도 파일 저장소(Supabase Storage) 없이 기존 DB 접근 권한 그대로 보호됨.
2. **목록 조회 성능 보호**: `get_all_clients()`(회원 목록/통계 등에서 씀)는 `photo_data`를 아예 빼고 조회하도록 분리(`_LIST_COLUMNS`). 사진은 회원 한 명을 볼 때(`get_client`)만 같이 옴 - 목록에 회원이 늘어나도 사진 때문에 느려지지 않음.
3. **자동 문서화**: 새 모듈 `document.py` - Jinja2로 회원 정보(사진 포함)를 인쇄용 HTML로 만듦. PDF 라이브러리를 새로 설치하지 않고, 직원이 다운로드한 HTML을 브라우저에서 열어 인쇄(Ctrl+P) → "PDF로 저장"하면 그대로 서류/PDF가 되는 방식. 새 메뉴 "회원 서류 출력" 추가 (미리보기 + 다운로드).
4. **보안 정리**: 회원 목록/단건조회/주소검색/통계 API에 로그인 필요(`Depends(auth.require_user)`)를 빠짐없이 추가함 - 지금까지는 이 엔드포인트들이 로그인 없이도 호출 가능했던 빈틈이 있었음(사진처럼 민감한 정보가 추가되는 김에 같이 막음).

**왜**: 피드백 #8 — "회원 등록 시 사진 촬영 기능, 서류로 자동 문서화."

**검증**: `sql/006_add_photo.sql`을 아직 실행 전이라(자는 동안이라 실행 못 함) 실제 사진 저장은 끝까지 테스트 못 했지만, ① 테스트 이미지를 base64로 만들어 등록 시도 → 예상대로 "컬럼 없음" 오류로 실패하는 것 확인(마이그레이션 필요성 재확인), ② **사진 없는 기존 회원으로 서류 생성은 정상 동작 확인**(HTML 200 응답, "사진 없음" 표시 정상) - `photo_data` 컬럼이 아직 없어도 기존 기능(목록/조회/서류생성)이 전혀 안 깨지는 것까지 확인함(설계상 의도된 하위호환).

**⚠️ 다음에 꼭 해야 할 것**: Supabase SQL Editor에서 `sql/006_add_photo.sql` 실행 필요 - 이거 하기 전엔 사진 촬영 후 등록/수정 시 "회원 등록 중 오류" 남. 실행 후 브라우저에서 카메라 촬영 → 등록 → "회원 서류 출력" 메뉴에서 확인 부탁.

**참고 개념**:
- **base64 인코딩**: 사진 같은 이진 데이터(바이트)를 텍스트로 바꾸는 방법입니다. DB의 일반 텍스트 컬럼에 사진을 "글자처럼" 저장할 수 있게 해줍니다. 대신 원본보다 텍스트 길이가 좀 더 길어집니다(그래서 미리 리사이즈해서 크기를 줄여둠).
- **서버 렌더링 문서 → 인쇄 → PDF**: 전용 PDF 생성 라이브러리(reportlab 등)는 시스템에 추가 설치가 필요할 수 있어 비전공자가 유지보수하기 번거로울 수 있습니다. 대신 "브라우저가 이미 가진 인쇄 기능"을 활용하면, 웹 페이지(HTML)를 그대로 PDF로 바꿀 수 있어서 새로운 도구를 배우거나 설치할 필요가 없습니다.

**후속 변경 (2026-08-14)**: 외부 테스트(ngrok) 중 실제 종이 서류(이용신청서/이용서약서/개인정보동의서 3종) 사진을 보고 요구사항이 구체화됨 — 문서를 HTML(브라우저 인쇄) 대신 **.docx(워드)**로 바꾸고, 오프라인 신청서에 있던 항목들을 온라인 등록 절차에 맞게 추가함. 아래 새 항목 참고.

---

## 2026-08-14 — 회원 등록 신청서/개인정보 동의서를 실제 종이 양식 기준으로 재작성 + docx 전환 + 엑셀등록 통합

**무엇을**:
1. **문서 형식 전환**: `document.py`를 Jinja2/HTML에서 `python-docx` 기반으로 새로 작성. 결과물은 `.docx` 파일 하나(A4, 2페이지: 1p=회원 등록 신청서, 2p=개인정보 등 수집 및 이용 동의서). `main.py`의 서류 엔드포인트가 이제 docx 파일을 돌려주고, 프론트 "회원 서류 출력" 메뉴도 `.docx` 다운로드 버튼으로 변경(HTML 미리보기 iframe 제거).
2. **DB에 새 항목 9개 추가** (`sql/007_add_intake_fields.sql`): `emergency_contact_name/relation/phone`(비상연락망), `join_route`(가입경로), `has_illness`/`illness_type`(질병유무/종류), `has_career`/`career_type`(경력·전직업 유무/종류), `counselor`(상담자). 기존 종이 "이용신청서" 양식에는 있었지만 온라인 등록 화면엔 없던 항목들 - 사용자가 실제 서식 사진 3장을 보내줘서 확인 후 반영함.
3. **등록/수정 화면에 위 항목 입력 UI 추가**: 비상연락망 3칸, 가입경로(선택지+기타 사유), 질병유무(있다면 종류 다중선택+기타), 경력유무(있다면 직업군 다중선택+기타), 상담자(담당자명).
4. **"동거형태"는 별도 필드를 새로 안 만들고 기존 "가구유형"을 재사용**하기로 함 - 종이 양식의 동거형태(독거/노인부부/자녀가족/조손가정/기타)와 이미 만들어둔 가구유형(독거노인/노인부부/조손가정 등)이 개념이 겹쳐서, 같은 걸 두 번 입력받지 않게 하려고 문서의 "동거형태" 칸에 가구유형 값을 그대로 표시함.
5. **개인정보 동의서(2페이지)는 사용자가 보내준 실제 양식 문구를 그대로 옮김** - 목적/수집항목/보유기간/동의거부권리를 표로 정리하고, 마지막 줄에 실제 동의 여부(☑/☐)를 회원이 등록 시 체크한 값 그대로 반영. 기관명("구립응암노인복지관")은 `document.py` 맨 위 `ORG_NAME` 상수 하나만 바꾸면 전체 문서에 반영되도록 함.
6. **관심있는 서비스/관심있는 것은?/상담결과는 빈 칸으로, "특이사항"은 제외** - 사용자 요청대로.
7. **엑셀 일괄등록을 "신규 회원 등록" 화면 안으로 이동** (`st.expander`로) - 더 이상 별도 사이드바 메뉴가 아니라, 회원 한 명 등록 폼 아래 같은 페이지에 있음. 메뉴 이동 없이 등록 방식만 바꿔 쓸 수 있음.

**왜**: 사용자가 실제 복지관에서 쓰는 종이 서식(이용신청서/이용서약서/개인정보동의서) 사진을 보내주며, 온라인 등록에도 이 내용이 반영되어야 한다고 요청. 이용서약서는 지금 단계에서 불필요하다고 판단해 제외. 문서 형식은 "다운로드 없는 인쇄"가 아니라 "다운로드하는 docx"로 확정(사용자 확인).

**검증**: 실제 API로 회원 등록(비상연락망/가입경로/질병/경력/상담자 값 포함) → 문서 생성 → python-docx로 표 내용 다시 읽어서 입력한 값이 문서에 정확히 반영되는지 전부 확인. 동의 여부(☑/☐) 체크마크도 저장된 동의 값에 맞게 정확히 표시되는 것 확인.

**버그 수정**: docx 파일명에 한글이 들어간 `Content-Disposition` 헤더 때문에 500 오류 발생 → HTTP 헤더는 영문(ASCII/latin-1)만 담을 수 있어서, RFC 5987 방식(`filename*=UTF-8''...`)으로 인코딩하도록 수정.

**⚠️ 다음 작업(노트북 포함) 전 필수**: Supabase SQL Editor에서 `sql/007_add_intake_fields.sql` 실행 필요(이미 이 세션에서 실행 완료함, 다음 컴퓨터에도 반영 필요).

**참고 개념**:
- **python-docx**: 파이썬으로 워드 문서(.docx)를 직접 만드는 라이브러리입니다. HTML+브라우저 인쇄 방식과 달리, 표/사진이 들어간 정식 문서 파일을 바로 만들어내고, 받는 사람이 워드나 한글에서 열어 편집도 할 수 있습니다.
- **HTTP 헤더 인코딩 제약**: 웹 요청/응답의 "헤더"(부가 정보를 담는 부분, 여기선 파일명)는 옛날 규격 때문에 영어/숫자만 안전하게 담을 수 있습니다. 한글 파일명을 보내려면 `RFC 5987`이라는 표준 방식으로 별도 인코딩해서 넣어야 합니다 - 이런 자잘한 규격 문제는 실제로 겪어보기 전엔 알기 어려운, 전형적인 "웹 표준 때문에 생기는 버그"입니다.

---

## 2026-08-14 — 서류/등록화면 다듬기: 중복 항목 정리, 동의내용 요약 표시, 서명 캔버스

**무엇을**:
1. 서류 1페이지에서 "관심있는 서비스"와 "관심있는 것은?"이 내용이 겹쳐서 "관심있는 것은?"만 남기고 제거.
2. `document.py`의 개인정보 동의서 문구(수집목적/항목/보유기간/제3자제공/초상권)를 함수(`personal_info_rows()`, `third_party_rows()`, `portrait_rows()`)로 분리해서 docx 문서와 등록 화면이 **같은 소스**를 쓰도록 함. 등록 화면 체크박스 위에 `document.consent_summary_markdown()`로 요약본을 표시(예전엔 체크박스 한 줄짜리 문구만 있어서, 실제로 뭘 동의하는지 화면에서는 안 보였음).
3. **서명 캔버스** 추가 - `streamlit-drawable-canvas` 라이브러리로 등록/수정 화면에 손가락·마우스 서명 가능한 캔버스 도입. `sql/008_add_signature.sql`로 `clients.signature_data`(base64 PNG) 컬럼 추가 - **서명여부를 별도 컬럼으로 안 만들고, 이 컬럼에 값이 있는지 없는지로 판단**(있으면 서명함, 비어있으면 서명 안 함). 사진 처리 때와 같은 이유: "서명함/안함" 플래그와 실제 서명 이미지를 각각 다른 컬럼에 두면 둘이 서로 어긋날 수 있어서, 사실을 한 곳에만 저장하는 게 더 안전합니다. 서명 이미지는 서류 2페이지 우측 하단(신청인란)에 삽입됨.

**왜**: 사용자가 서류를 직접 확인해보고 준 피드백 3건 — (1) 관심서비스/관심있는것 중복, (2) 등록화면에 동의 내용이 요약조차 안 보이는 문제, (3) 로컬 테스트라 생략했던 서명란을 이제 태블릿/모바일에서 실제로 받아야 함.

**검증**: 실제 API로 서명 이미지(base64 PNG) 포함 등록 → 문서 생성 → docx 내부에 이미지 파일(`word/media/image1.png`)이 실제로 삽입된 것 확인. 등록/삭제로 테스트 데이터 정리.

**⚠️ 검증 못한 부분**: `streamlit-drawable-canvas`는 커뮤니티 컴포넌트라 실제 브라우저에서 캔버스가 정상적으로 그려지고 서명이 잡히는지는 이 세션에서 직접 확인 못함(브라우저 조작 불가) - 사용자가 브라우저에서 직접 확인 필요.

**참고 개념**:
- **단일 진실 공급원(single source of truth)**: 같은 정보(여기선 동의서 문구)를 여러 곳에 각각 따로 적어두면, 나중에 한쪽만 고치고 다른 쪽을 깜빡해서 서로 달라지는 사고가 잘 납니다. 그래서 문구를 함수로 한 번만 정의해두고, docx 문서와 화면 둘 다 그 함수를 가져다 쓰게 만들었습니다.
- **"사실 하나는 한 곳에만 저장"**: `signature_data` 컬럼 하나로 "서명 이미지"와 "서명 여부"를 동시에 표현한 것도 같은 원리입니다. `has_disability`처럼 이미 "있다/없다" 형태를 쓰던 다른 항목들과는 다르게, 여기서는 이미지 데이터 자체가 "있음/없음"을 자연스럽게 나타낼 수 있어서 컬럼을 하나 아꼈습니다.

**후속 수정 (2026-08-14) — 서명 캔버스가 실제로는 안 그려짐**: 사용자가 브라우저에서 확인해보니 캔버스 자체가 안 나타남. 원인을 두 단계로 좁힘:
1. 1차 시도(`streamlit-drawable-canvas`)는 오래된 서드파티 라이브러리라 최신 Streamlit과 호환 안 됨 → 제거.
2. 그래서 순수 HTML/JS로 직접 만들었는데, 이때 참고한 "Streamlit 커스텀 컴포넌트 만드는 법"이 **구버전(v1) 방식**이었음(외부 CDN에서 `streamlit-component-lib.js`를 불러와 `window.Streamlit.setComponentValue()`를 쓰는 방식) - 그런데 이 프로젝트의 Streamlit 버전(1.59)은 컴포넌트 제작 방식이 완전히 새로운 버전(Custom Components v2)으로 바뀌어서, v1 방식 자체가 동작하지 않음(심지어 CDN 주소도 실제로 존재하지 않는 404 주소였음). Streamlit 패키지 안에 내장된 공식 v2 가이드 문서를 직접 찾아 읽고, `st.components.v2.component(html=..., js=...)` 방식으로 다시 작성해서 해결.

**참고 개념 추가**:
- **라이브러리/프레임워크의 "메이저 버전 교체"**: 가끔 도구 자체가 예전 방식을 완전히 버리고 새 방식으로 바꾸는 경우가 있습니다(Streamlit의 컴포넌트 제작 방식이 v1→v2로 바뀐 것처럼). 이럴 때 인터넷의 예전 예제/블로그 글을 그대로 따라 하면 안 먹힙니다 - 지금 실제로 설치된 버전의 공식 문서를 확인하는 게 중요합니다(이번엔 마침 Streamlit 패키지 자체에 그 문서가 들어있어서 바로 찾아 확인함).

---

## 2026-08-14 — 등록 화면을 "하나의 폼"처럼 재배치

**무엇을**: 신규 회원 등록/회원 수정 화면을 실제 종이 이용신청서와 비슷한 흐름으로 재배치. 사진 촬영과 서명은 이제 메인 `st.form` **안**으로 들어갔고(사진|기본정보를 나란히, 아래로 인적사항→비상연락망→생활정보→동의→서명 순서), 펼쳐야만 보이던 `st.expander`들을 없애고 항상 보이는 배치로 바꿈.

**Streamlit 자체의 한계 하나 확인됨**: "주소 검색"과 "기존 등록 이력 확인"은 폼 안에 넣을 수 없었음 - `st.form` 안에는 `st.form_submit_button` 외의 일반 버튼(`st.button`)을 둘 수 없다는 것이 Streamlit의 공식 제약이라, 검색 버튼이 필요한 이 두 기능은 폼 바깥에 남겨두고, 대신 펼침 클릭 없이 항상 열려있는 형태로 만들어 최대한 자연스럽게 이어지도록 배치함.

**왜**: 사용자가 실제로 화면을 써보고 "기능을 하나씩 추가하다 보니 배치가 산만하다, 클릭해서 펼쳐야 하는 게 불편하다"는 피드백을 줌.

---

## 2026-08-14 — 목록 한글 매핑 정리, 서류 기관명 익명화, 통계 화면에 그래프 추가

**무엇을**:
1. `app_frontend.py`의 `COLUMN_LABELS`에 최근 추가된 항목들(비상연락망, 가입경로, 질병/경력 유무·종류, 상담자) 한글 라벨 추가. 존재하지 않는 컬럼이었던 `manager` 매핑(죽은 코드)은 정리. `deleted_at`처럼 화면에 보여줄 필요 없는 내부 관리용 컬럼은 `HIDDEN_COLUMNS`로 분리해서 목록/삭제 화면에서 뺌. 회원 삭제 화면의 상세 정보 표에서도 `photo_data`/`signature_data`(base64 문자열)를 표에서 빼고, 사진은 이미지로 따로 보여주도록 정리.
2. `document.py`의 `ORG_NAME`을 실제 기관명("구립응암노인복지관")에서 "테스트용 문서입니다"로 변경 - 테스트 단계에서 서류에 실제 기관명이 노출되지 않도록. 문장 중간에 기관명이 끼어 있던 곳들은 자연스럽게 읽히도록 문구를 살짝 조정(기관명 없이도 말이 되게).
3. `stats.py`에 `build_household_type_distribution`(가구 유형별 인원, 다중선택이라 콤마 분리해서 집계), `build_disability_distribution` 함수 추가. 이미 있었지만 한 번도 안 쓰이고 있던 `build_age_distribution`/`build_period_trend`도 이번에 처음 연결함. `main.py`에 `/api/stats/charts` 엔드포인트 추가(위 통계들을 한 번에 모아서 반환). 통계 화면에 `st.metric`(핵심 숫자 카드), 월별 신규가입 추이(꺾은선), 성별/연령대/장애유무/회원구분/가구유형 분포(막대) 그래프 추가 - Streamlit 기본 차트 기능만 써서 새 라이브러리 설치 없이 구현.

**왜**: 사용자 피드백 3건 — (1) 새 컬럼들이 화면에 영어 원본 이름으로 보이거나 아예 안 보임, (2) 테스트 단계에서 서류에 실제 기관명이 나가면 안 됨, (3) 통계가 표만 있어서 한눈에 안 들어옴 + 관리자에게 유용한 지표 보강.

**검증**: `/api/stats/charts`를 curl로 호출해 5개 통계 항목이 실제 등록된 회원 데이터를 정확히 반영하는 것 확인(예: 가구유형은 중복 집계되어 합계가 전체 회원수보다 클 수 있음 - 의도된 동작). 서류 재생성해서 기관명이 "테스트용 문서입니다"로만 나오고 문장들도 자연스럽게 읽히는 것 확인.

**참고 개념**:
- **"이미 있는데 안 쓰이던 코드"**: `build_age_distribution`, `build_period_trend`, 그리고 이전에 발견한 `check_duplicates` API처럼, 코드베이스에 이미 만들어져 있지만 화면 어디에서도 호출되지 않는 함수들이 종종 있습니다. 새 기능을 만들기 전에 먼저 "혹시 이미 준비되어 있나?"를 확인하면 중복 작업을 피할 수 있습니다.
- **다중 선택 항목의 통계 집계**: "가구 유형"처럼 한 사람이 여러 값을 가질 수 있는 항목은, 단순히 인원수를 세면 안 되고 각 값을 낱개로 풀어서(콤마 분리) 세야 합니다. 그래서 이 통계의 합계는 전체 회원 수보다 클 수 있는데, 이건 버그가 아니라 "한 사람이 여러 카테고리에 동시에 속할 수 있다"는 걸 정확히 반영한 결과입니다.

---

## 2026-08-14 — #5 UI 전면 개편: Streamlit → FastAPI + Jinja2 + CSS

**무엇을**: 마지막으로 남아있던 피드백(#5, UI 전면 개편)을 처리했습니다. 화면(프론트엔드)을 Streamlit에서 완전히 걷어내고, FastAPI + Jinja2(HTML 템플릿) + 순수 CSS/JS로 새로 만들었습니다. 백엔드 로직(Supabase 연동, docx 생성, AI 추천, 엑셀 파싱)은 대부분 그대로 재사용했고, 화면만 새로 만든 작업입니다.

1. **시크릿 분리**: `auth.py`/`supabase_db.py`/`address_api.py`/`gov_welfare_api.py`/`ai_recommend.py`/`welfare_search.py` 6개 파일이 전부 `import streamlit as st`로 `.streamlit/secrets.toml`을 읽고 있었습니다(FastAPI 백엔드라면서 실제로는 Streamlit 없이는 동작 못 하는 상태였던 것 - 이번에 처음 발견). `config.py`(python-dotenv 기반) + `.env` 파일로 교체했고, `gov_welfare_api.py`/`welfare_search.py`가 쓰던 `st.cache_data(ttl=...)`(같은 조건이면 API를 다시 안 부르는 캐시)도 `ttl_cache.py`라는 작은 대체 모듈로 옮겼습니다.
2. **쿠키 기반 로그인 유지**: `main.py`에 `Jinja2Templates`+`StaticFiles`를 붙이고, `access_token`/`refresh_token`을 httpOnly 쿠키로 관리하는 화면 전용 로그인 흐름(`get_page_user`)을 새로 만들었습니다. `auth.require_user`도 `Authorization` 헤더뿐 아니라 쿠키에서도 토큰을 찾도록 넓혀서, 화면의 JS `fetch()` 호출이 별도 토큰 처리 없이 그냥 쿠키로 인증되게 했습니다. 쿠키가 만료되면 `/api/session/refresh`를 호출해 조용히 갱신 후 재시도하는 걸 `static/js/api.js`의 공통 fetch 래퍼로 만들었습니다.
3. **서명 캔버스 재작성**: Streamlit Custom Components였던 `signature_pad.py`를 삭제하고, 순수 `<canvas>` + `static/js/signature-pad.js`(바닐라 JS)로 바꿨습니다. 드로잉 로직 자체는 그대로 옮겼습니다.
4. **디자인**: `static/css/style.css`에 베이지/우드톤 배경 + 그린 포인트 색상, 둥근 카드/버튼, Google Fonts "Gowun Dodum"(둥글둥글한 한글 폰트)을 적용해 "전문적"이기보다 "따뜻하고 친근한" 느낌을 내려 했습니다. 상단 네비게이션은 가로 스크롤 되는 알약 모양 탭으로 만들어서, 데스크톱/태블릿/모바일 어디서든 같은 방식으로 쓸 수 있게 했습니다.
5. **화면 7개 + 로그인** 전부 새로 작성: 회원 목록, 신규 등록(기존 이력 확인 → 주소 검색 → 사진·서명 포함 등록폼 → 엑셀 일괄등록까지 한 화면), 회원 수정, 회원 삭제(관리자 전용), 서류 출력, 통계(Chart.js로 그래프), 추천 복지 서비스(AI 챗봇, 2초 간격 polling으로 백그라운드 작업 진행상황 확인 - 예전 `time.sleep(2)+rerun` 방식을 브라우저 쪽 `setInterval`로 대체).
6. **엑셀 일괄등록 개선**: 예전엔 화면(Streamlit)이 한 줄씩 등록 API를 반복 호출했는데, 이번에 `POST /api/bulk/preview`(미리보기)와 `POST /api/clients/bulk`(일괄 처리)를 새로 만들어서 서버가 파일 전체를 한 번에 처리하도록 옮겼습니다. 더 안정적입니다.
7. `app_frontend.py`, `signature_pad.py` 삭제. `requirements.txt`에서 `streamlit`/`extra-streamlit-components` 제거, `python-dotenv`/`python-multipart` 추가. `.gitignore`에 `templates/`, `static/` 허용 규칙 추가. README.md도 새 구조에 맞게 갱신.

**실행 방법이 바뀝니다**: 예전엔 백엔드(`uvicorn`)와 프론트(`streamlit run`) 두 개를 따로 켜야 했는데, 이제 `uvicorn main:app --reload` **하나만** 켜면 `http://localhost:8000`에서 전체 화면을 쓸 수 있습니다.

**왜**: 사용자가 "복지시설이 쓰는 서비스니까 따뜻한 느낌이면 좋겠고, 기능을 한눈에 찾을 수 있도록 직관적이면 좋겠다, 전문적인 느낌보다는 친화적인 느낌"을 요청. Streamlit은 화면 배치·디자인 자유도가 낮아서 이 요청을 제대로 만족시키기 어려웠습니다.

**검증**: 브라우저 조작은 이번에도 직접 못 했지만(터미널 환경), curl로 실제 서버를 띄워 전체 흐름을 끝까지 확인했습니다 — 로그인→쿠키 발급→쿠키만으로 `/api/*` 인증 성공→회원 등록→서류(.docx) 생성 확인(38KB 정상 생성)→수정 화면 진입→삭제, 그리고 refresh_token만 남기고 access_token을 지운 상태에서 `/api/session/refresh` 호출 시 정상적으로 재로그인 없이 갱신되는 것, 쿠키가 아예 없을 때 `/members` 접근 시 `/login`으로 리다이렉트되는 것, 엑셀 파일 업로드→미리보기→일괄 등록(2건 성공)까지 전부 확인했습니다. 7개 화면 모두 Jinja2 템플릿 렌더링 오류 없이 200 응답되는 것도 확인했습니다.

**⚠️ 검증 못한 부분(사용자 확인 필요)**: 이번에도 실제 브라우저 조작은 할 수 없었습니다. 특히 아래는 반드시 브라우저(가능하면 태블릿/모바일 포함)에서 직접 확인해주세요:
- 서명 캔버스가 터치/마우스로 실제로 잘 그려지는지 (드로잉 로직은 예전 것과 동일하지만, Streamlit 컴포넌트가 아닌 일반 페이지 안에서 처음 테스트하는 것)
- 전체적인 디자인 톤("따뜻하고 친근한 느낌")이 의도한 대로 나오는지, 베이지/그린 배색이 마음에 드는지
- 상단 메뉴 탭이 모바일 화면 너비에서 가로 스크롤로 잘 보이는지
- 사진 촬영이 태블릿/모바일에서 실제 카메라를 잘 여는지 (`<input type=file capture>` 방식으로 바뀜 - Streamlit의 `st.camera_input`과는 다른 방식)

**참고 개념**:
- **Jinja2**: 파이썬 문자열에 `{{ 변수 }}`, `{% if %}` 같은 문법을 섞어서 HTML을 동적으로 만들어주는 템플릿 엔진입니다. FastAPI가 API 응답(JSON)뿐 아니라 사람이 보는 화면(HTML)까지 만들 수 있게 해줍니다.
- **httpOnly 쿠키**: 브라우저에 저장되지만 JavaScript 코드로는 읽을 수 없는 쿠키입니다. 로그인 토큰을 이 방식으로 저장하면, 페이지에 악성 스크립트가 끼어들어도 토큰을 훔쳐갈 수 없어서 더 안전합니다.
- **폴링(polling)**: 서버에 "아직 안 끝났어?"를 주기적으로 물어보는 방식입니다. 진짜 실시간 알림(웹소켓 등)보다는 단순하지만, 이 정도 규모(직원 몇 명이 쓰는 앱)에서는 2초 간격 polling만으로 충분합니다.

---

## 2026-08-14 — 표(table)를 카드형 목록으로 교체

**무엇을**: 사용자가 실제로 화면을 열어보고 "전체적으로 데이터프레임(엑셀표) 같다"는 피드백을 줌. 표 태그(`<table>`)로 만들었던 화면 3곳을 카드형 목록으로 바꿨습니다.
1. **회원 목록**: 행/칸으로 나열되던 표 → 회원 한 명당 카드 하나(이름 첫 글자 아바타, 이름/회원번호, 성별·회원구분·장애여부 배지, 주소·전화·가입일). 카드를 누르면 바로 그 회원의 수정 화면으로 이동합니다.
2. **회원 삭제 상세**: key-value 표 → 프로필 카드(아바타/이름 상단 + 나머지 정보는 좌우로 정렬된 라벨/값 목록). 값이 비어있는 항목은 아예 안 보이게 해서 더 깔끔해졌습니다.
3. **통계 - 월별 가입 현황**: 표 → 달마다 하나씩, 가로로 스크롤되는 통계 카드(신규가입 인원수를 크게, 성별/회원구분 세부는 작게).
4. AI 추천 화면의 "조회된 정부 복지서비스" 표도 같은 이유로 카드 목록으로 바꿨습니다.

엑셀 일괄등록 화면의 "미리보기" 표는 그대로 뒀습니다 - 그건 실제 업로드한 엑셀 파일 내용을 그대로 보여주는 용도라(진짜 스프레드시트 미리보기), 표 모양이 오히려 자연스럽습니다.

**왜**: `<table>`은 태생적으로 "데이터를 격자로 촘촘히 나열"하는 모양이라 여러 줄일수록 엑셀/스프레드시트처럼 보입니다. 사용자가 원한 "따뜻하고 직관적인" 느낌과는 거리가 있어서, 정보량이 많지 않은 화면들은 카드형으로 바꿨습니다.

**검증**: `style.css`에 카드형 목록용 클래스(`.card-grid`, `.entity-card`, `.avatar`, `.badge`, `.detail-grid`, `.period-strip` 등) 추가 후, curl로 `/members` 페이지가 200으로 렌더링되는 것과 `/api/clients`가 카드 렌더링에 필요한 필드(`id`, `name`, `member_no`, `gender`, `welfare_type` 등)를 실제로 돌려주는 것 확인. 다른 화면들도 템플릿 렌더링 오류 없이 전부 200 확인.

**⚠️ 검증 못한 부분**: 역시 실제 브라우저로 카드 배치가 보기 좋게 나오는지는 직접 확인이 필요합니다.

**참고 개념**:
- **표(table) vs 카드(card) 레이아웃**: 표는 같은 종류의 값을 줄지어 비교하기엔 좋지만(그래서 엑셀 느낌이 남), 사람 한 명 한 명을 "하나의 단위"로 보여줄 때는 사진·이름·핵심 정보를 한 덩어리로 묶은 카드가 더 자연스럽게 읽힙니다. 어떤 화면이 표가 어울리고 어떤 화면이 카드가 어울리는지는 "이 데이터를 서로 비교하려고 보는가, 한 명씩 살펴보려고 보는가"로 구분하면 됩니다.

---

## 2026-08-14 — 디자인 시스템 전면 재적용: Tailwind + Alpine + Lucide

**무엇을**: 사용자가 이번엔 아주 구체적인 디자인 프롬프트를 직접 작성해서 전달함(컬러 코드, 폰트, 레이아웃 구조, 아이콘 라이브러리까지 명시). 그 문서를 그대로 분석해서 디자인 시스템을 다시 세우고, 이미 만든 화면 전부(로그인/대시보드/목록/등록/수정/삭제/서류출력/통계/추천)에 일괄 적용했습니다.

1. **색상·폰트 토큰**: 오렌지(`#F97316`) + 그린(`#22C55E`) 포인트, 아이보리 배경(`#FFFBF5`), Pretendard 폰트로 전면 교체(예전 베이지/우드톤/Gowun Dodum 조합 폐기). `templates/base.html`의 `<head>`에 `tailwind.config`로 인라인 정의.
2. **레이아웃**: 사이드바(좌측 고정, 메뉴별 Lucide 아이콘) + 상단 헤더(현재 화면 이름, 로그인 정보, 로그아웃) + 본문 구조로 전면 개편. 모바일 폭에서는 Alpine.js로 사이드바를 오프캔버스 메뉴로 전환.
3. **대시보드(홈) 신설**: 로그인 직후 보이던 화면을 회원 목록에서 대시보드로 바꿈. 인사말+오늘 날짜, 핵심 지표 카드, 기능별 바로가기 카드(회원 등록/목록/서류출력/통계/추천 + 관리자 전용 삭제)로 구성.
4. **회원 목록에 정렬 추가**: 검색 옆에 "최근등록순/이름순" 정렬을 추가(요청에 있던 "정렬·필터가 잘 보이는 위치" 반영).
5. **빈 상태(empty state) 통일**: "등록된 회원이 없습니다" 같은 문구에 아이콘+안내문 형태를 통일 적용.
6. **컴포넌트 매크로**: `templates/_macros.html`에 버튼/뱃지/빈상태/카드 매크로를 만들어 재사용 구조 마련.

**기술적으로 한 가지 중요한 선택**: Tailwind CSS는 보통 `npm install` + 빌드 과정이 필요한데, 이 프로젝트는 비전공자가 `uvicorn main:app` 하나만 켜면 되는 구조를 유지해야 해서 **빌드 없이 쓰는 CDN 방식(Play CDN)**을 썼습니다. 대신 회원 카드·뱃지·채팅 말풍선처럼 JavaScript가 나중에 다시 찾아서 조작해야 하는 요소들은 `static/css/custom.css`에 이름 있는 클래스(`.btn`, `.card`, `.badge`, `.entity-card` 등)로 새 색상 기준에 맞춰 남겨뒀습니다. 즉, "배치와 여백"은 Tailwind 클래스로, "부품의 생김새"는 `custom.css`로 나눠서 관리합니다. 예전 `style.css`는 삭제했습니다.

**"회원 상세(탭)" 화면은 만들지 않음**: 사용자의 프롬프트 제안 중 "기본정보·상담기록·서비스이력" 탭 구조가 있었는데, 지금 시스템엔 상담기록/서비스이력을 저장하는 기능 자체가 없어서(회원 등록 정보와 AI 추천 기능만 있음), 없는 데이터를 위한 빈 탭을 만드는 대신 기존처럼 "회원 수정" 화면 하나로 정보를 보고 고치는 구조를 유지하기로 사용자와 확인 후 진행했습니다.

**왜**: 지난 개편안(베이지/그린 톤)을 사용자가 실제로 써보고 "더 직관적이긴 한데 원하는 느낌이 아니다"라고 판단, 훨씬 더 구체적인 기준을 직접 제시함.

**검증**: curl로 로그인 → 대시보드/목록/등록/수정/삭제/서류출력/통계/추천 전체 화면이 실제 서버(쿠키 인증 포함)에서 200으로 렌더링되는 것 확인. `uvicorn --reload`가 파일 저장마다 자동 반영되는 것도 로그로 확인(에러 없이 재시작됨).

**⚠️ 검증 못한 부분**: 이번에도 실제 브라우저 조작은 할 수 없었습니다. 특히 색감·여백이 실제로 "따뜻한 관공서스럽지 않은 느낌"으로 보이는지, 모바일 폭에서 사이드바 토글이 매끄러운지, 서명 캔버스/카메라가 여전히 잘 동작하는지는 브라우저에서 직접 확인이 필요합니다.

**참고 개념**:
- **Tailwind Play CDN**: 원래 Tailwind는 프로젝트를 빌드(컴파일)해서 필요한 CSS만 뽑아내는 방식이 정석이지만, `<script src="https://cdn.tailwindcss.com">` 한 줄만 넣으면 브라우저가 그 자리에서 바로 style을 만들어주는 "빌드 없는" 버전도 있습니다. 이 프로젝트처럼 빌드 도구 설치 자체가 부담인 경우에 적합합니다(다만 매우 큰 트래픽의 실서비스에는 권장되지 않는데, 이 앱은 직원 몇 명이 쓰는 내부 도구라 상관없습니다).
- **Alpine.js**: React/Vue 같은 무거운 프레임워크 없이, HTML 태그에 `x-data`, `@click` 같은 속성만 추가하면 그 자리에서 바로 상태를 다루게 해주는 아주 가벼운 라이브러리입니다. 여기서는 "사이드바 열고 닫기" 딱 한 가지 용도로만 씁니다.

---

## 2026-08-14 — 실사용 피드백 대량 반영 (검색창 버그 수정, 화면별 UX 개선 9건)

**무엇을**:
1. **대시보드 인사말 단순화**: 이메일을 그대로 노출하던 인사말을 간단한 문구로 교체(신원 정보는 이미 상단 헤더에 있음).
2. **회원목록 검색창 아이콘 겹침 버그 수정**: 돋보기 아이콘과 "이름으로 검색" placeholder 글자가 겹쳐 보이던 문제. 원인은 `custom.css`에 `input[type="text"]{padding:9px 12px}`를 일반 선택자로 줘서, 템플릿에 쓴 Tailwind의 `pl-9`(아이콘 자리 확보용 왼쪽 여백) 유틸리티 클래스보다 CSS 우선순위(specificity)가 더 높았던 것 — 그래서 `pl-9`가 항상 무시되고 있었습니다. `:where(...)`로 감싸서 이 기본 규칙의 우선순위를 0으로 낮춰, 앞으로 어떤 Tailwind 유틸리티 클래스를 붙여도 항상 이길 수 있도록 근본 원인을 고쳤습니다(이 버그가 다른 화면에서 또 나올 수 있었는데 한 번에 막음).
3. **회원수정 화면에 동의 현황 표시**: 개인정보 동의는 최초 등록 시에만 받는 게 맞지만, 지금까지 뭘 동의했는지/언제 동의했는지 수정 화면에서는 전혀 안 보였음 → 4개 동의 항목(개인정보/민감정보/제3자제공/초상권) + 동의일시를 읽기 전용 뱃지로 표시(수정은 불가, 확인만 가능).
4. **서류 출력 기능을 회원수정 화면에도 통합**: 별도 "서류 출력" 메뉴는 그대로 두고, 회원수정 화면에도 "등록 서류(.docx) 만들기" 버튼을 추가해 굳이 메뉴를 옮기지 않아도 그 자리에서 서류를 받을 수 있게 함.
5. **신규 등록 폼에 주소 검색 통합**: 예전 Streamlit 버전은 "폼 안에는 st.form_submit_button 외의 일반 버튼을 못 둔다"는 제약 때문에 주소 검색이 폼 밖 별도 카드로 분리돼 있었음. 지금은 순수 HTML 폼이라 그 제약이 없어서, 주소 입력창 바로 옆에 검색 버튼을 붙이고 검색 결과를 그 아래 바로 띄우는 방식으로 하나로 합쳤습니다(검색어=주소 입력창 자체, 결과를 고르면 같은 칸이 채워짐).
6. **기존등록 이력 확인 + 엑셀 일괄등록을 상단에 나란히 배치**: 예전엔 엑셀 일괄등록이 폼 맨 아래에 있어 스크롤을 끝까지 내려야 보였음 → 두 보조 기능(이력 확인, 일괄등록)을 폼 위 2단 그리드로 옮겨서 스크롤 없이 바로 보이게 함.
7. **회원수정/삭제/서류출력 화면을 드롭다운 → 이름 검색으로 교체**: 회원이 많아지면 드롭다운에서 스크롤해서 찾기 불편해짐 → `static/js/member-search.js` 공용 위젯을 만들어, 이름을 치면 바로 아래 일치하는 회원이 목록으로 뜨고 클릭해서 고르는 방식으로 3개 화면 모두 교체.
8. **통계 화면 확장**:
   - 차트 크기가 항목 개수에 따라 제각각으로 보이던 문제 → 모든 차트를 `.chart-box`(고정 높이) + Chart.js `maintainAspectRatio:false` 조합으로 통일.
   - 질병 유무/경력 유무/가입경로 통계를 새로 추가(`stats.py`에 `build_illness_distribution`, `build_career_distribution`, `build_join_route_distribution` 추가) — 이제 회원 등록 시 입력하는 항목 중 통계 낼 수 있는 건 거의 다 그래프로 보임.
   - "월별 가입 현황"을 일/월/년 탭으로 전환 가능하게 만듦(사실 백엔드 `build_period_breakdown`는 이미 세 단위를 다 지원하고 있었고, 화면에 탭만 없었던 것 — 백엔드 수정 없이 프론트만 추가).
   - 일별 탭을 고르면 날짜 선택창이 나타나 특정 날짜만 필터링해서 볼 수 있음.
9. **전체 화면 여백 축소 + 폭 확장**: `base.html`의 본문 최대폭을 `max-w-5xl`(1024px)에서 `max-w-[1600px]`로 넓히고, 카드 내부 여백을 20px→16px로 줄여 스크롤이 덜 필요하도록 조정. (Tailwind 자체의 한계가 아니라 그냥 제가 처음에 폭을 너무 좁게 잡았던 것입니다.)

**왜**: 사용자가 브라우저에서 직접 써보고 화면마다 구체적인 불편함을 짚어준 피드백을 그대로 반영.

**검증**: curl로 전체 화면 200 렌더링 재확인, `/api/stats/charts`에 새 통계 3종이 실제로 포함되는 것, `/api/stats/period/일`이 날짜별 데이터를 정확히 돌려주는 것, 실제 회원 데이터에 동의 관련 필드가 정상적으로 들어있는 것 확인.

**참고 개념**:
- **CSS 우선순위(specificity)와 Tailwind 유틸리티 클래스의 충돌**: Tailwind 클래스(`.pl-9` 등)는 보통 선택자 하나짜리라 우선순위가 낮습니다. 그런데 직접 쓴 CSS에서 `input[type="text"]`처럼 태그+속성을 섞은 선택자를 쓰면 우선순위가 더 높아져서, 나중에 어떤 Tailwind 클래스를 추가해도 씹히는 일이 생깁니다. `:where(...)`로 감싸면 그 안의 선택자는 "우선순위 계산에서 제외"되어 이런 충돌을 원천적으로 막을 수 있습니다 — 커스텀 CSS로 기본값만 깔아두고 싶을 때 유용한 패턴입니다.

## 2026-08-14 — 홈 화면 통계 변경, 구분선/폰트 가독성 개선

**무엇을**:
1. **홈 화면 요약 통계 변경**: "전체/오늘 신규가입/이번달 신규가입/남·여/수급자"에서 가입 수(통계 화면에서 이미 확인 가능) 항목을 빼고 **전체/남/여/일반/수급자/차상위** 6개로 재구성.
2. **섹션 구분선을 진하게**: 등록/수정 화면의 "비상연락망", "생활 정보" 같은 구간 구분선이 연한 점선이라 잘 안 보였음 → 굵은 실선(2px, 진한 오렌지)으로 교체해서 어디까지가 한 구간인지 명확히 구분되게 함.
3. **전체 폰트 크기 확대**: `<html>`의 기본 글자 크기를 16px→18px로 올림. Tailwind 유틸리티 대부분과 이 프로젝트의 커스텀 CSS가 `rem` 단위를 쓰고 있어서, 이 한 줄로 화면 전체 글자 크기가 한 번에 비례해서 커짐(화면마다 일일이 클래스를 안 바꿔도 됨). 버튼 아이콘 크기도 16px→18px로 같이 키워 텍스트와 비율을 맞춤. 라디오 버튼 정렬용 고정 높이(38px→42px)도 커진 글자에 맞게 살짝 늘림.

**왜**: 사용자가 실제로 써보고 준 구체적 피드백. 특히 구분선/폰트는 "사용자 입장에서 불편한 점을 스스로 찾아서 고쳐달라"는 요청도 함께 있어서, 코드를 다시 훑어보며 아이콘-텍스트 비율처럼 사용자가 아직 언급하지 않았을 법한 자잘한 균형도 같이 다듬었습니다.

**검증**: curl로 전체 화면 재확인, `/api/stats/summary`가 male/female/general/near_poor/recipient 필드를 모두 정상적으로 돌려주는 것 확인.

**참고 개념**:
- **rem과 전체 크기 조절**: `rem`은 "루트(html 태그)의 글자 크기를 기준으로 한 배수" 단위입니다. `px`로 일일이 크기를 정하면 전체를 키우고 싶을 때 수백 곳을 다 고쳐야 하지만, `rem`으로 통일해두면 `html { font-size: 18px }` 한 줄만 바꿔도 그 배수로 정의된 모든 글자·간격이 한 번에 비례해서 커집니다.

## 2026-08-14 — 입력창 테두리가 안 보이던 문제 수정

**무엇을**: 사용자가 등록 화면 스크린샷을 보내줬는데, 입력창 테두리가 거의 안 보여서 어디에 입력하는지 눈으로 구분이 안 되는 문제였음. 원인은 입력창 테두리 색(`--border: #EAE3D6`, 연한 베이지)이 흰 배경(입력창)과 거의 비슷한 톤이라 대비가 거의 없었던 것. 카드/표 테두리용 색과 입력창 테두리용 색을 분리해서, 입력창 전용으로 `--input-border: #C7B48F`(더 진한 톤)를 새로 만들고 두께도 1px→1.5px로 키움.

**왜**: 카드나 표 테두리는 옅어도 배경(아이보리)과 흰 카드 사이에 이미 대비가 있어서 괜찮았지만, 입력창은 카드 위의 흰 배경이라 원래 테두리 색으로는 거의 안 보였음 - 용도가 다른데 같은 색 변수를 쓰고 있었던 게 원인.

**검증**: curl로 등록 화면 200 렌더링 확인.

## 2026-08-14 — 입력창 테두리 버그 진짜 원인 수정, 통계 화면 원형차트 전환

**무엇을**:
1. **입력창 테두리 버그, 이번엔 진짜 원인 파악**: 지난번 "고쳤다"고 한 게 실제로는 안 고쳐졌음. 원인을 다시 보니, 아이콘 겹침 버그를 고칠 때 `width`/`padding`뿐 아니라 `border`까지 통째로 같은 `:where(...)` 블록 안에 넣어버려서, 테두리 규칙의 우선순위까지 0으로 떨어뜨렸던 게 문제였음. 그러면 Tailwind CDN이 나중에 주입하는 리셋 스타일(모든 요소의 테두리를 0으로 만드는 것)과 우선순위가 똑같아지고, 어느 쪽이 나중에 로드되느냐에 따라 테두리가 사라질 수 있었음. `width`/`padding`(Tailwind 유틸리티가 덮어써야 하는 부분)과 `border`/`배경`/`색`(항상 우리가 이겨야 하는 부분)을 서로 다른 규칙으로 분리해서 확실히 고침.
2. **통계 화면을 막대→원형(도넛) 차트로 전환**: 월별 신규가입 추이는 막대 그래프로 유지, 나머지 분포(성별/회원구분/연령대/가구유형/장애/질병/경력/가입경로)는 전부 도넛 차트로 교체. `chartjs-plugin-datalabels`(CDN)를 추가해서 조각 위에 인원수+비율(%)이 직접 보이게 함(예: "3명 (43%)") - 범례만 봐서는 정확한 수치를 알기 어려웠던 문제 해결.
3. **통계 화면 순서 변경**: 전체 요약 통계 → 가입 현황(일/월/년 탭) → 월별 신규가입 추이 → 분포 차트들 순으로 재배치(원래는 가입 현황이 맨 아래에 있었음).
4. **"현재 기준" 날짜 표시**: 홈 화면과 통계 화면의 요약 통계 카드 위에 "현재 기준: 2026년 8월 14일"처럼 오늘 날짜를 표시해서, 이 숫자가 언제 시점 기준인지 명확하게 함.

**왜**: 사용자가 스크린샷으로 직접 확인해준 실제 버그(1번), 그리고 막대그래프보다 비율 데이터는 원형 차트가 한눈에 들어온다는 사용성 피드백.

**검증**: curl로 `/dashboard`, `/stats` 재렌더링 확인, chartjs-plugin-datalabels CDN 주소 접근 가능 확인. CSS 우선순위 관련 부분은 코드 리뷰로 재차 확인(이번엔 `border`가 더 이상 `:where()` 안에 있지 않음을 직접 확인).

**참고 개념**:
- **같은 버그를 "고쳤다"고 착각하기 쉬운 이유**: 지난번엔 겉보기엔 그럴듯한 수정(테두리 색을 더 진하게)을 했지만, 실제로 그 규칙 자체가 적용조차 안 되고 있었던 게 진짜 원인이었습니다. 값(색/두께)을 바꾸기 전에, 그 규칙이 애초에 화면에 반영되고 있는지(우선순위·로드 순서 문제는 없는지)부터 확인하는 게 중요합니다 - 이번엔 사용자가 "똑같다"고 정확히 알려준 덕분에 진짜 원인을 다시 찾을 수 있었습니다.

## 2026-08-14 — 검색창 통일, 메뉴명 변경, 통계 파이차트, 가독성/레이아웃 정리

**무엇을**:
1. **검색창 크기 통일**: 화면마다 검색창 너비가 제각각이던 걸 `.search-box`(최대 420px) 클래스로 통일해서 회원목록/정보수정/회원삭제/서류출력/맞춤형복지추천 5곳 모두 동일하게 맞춤.
2. **회원 선택 방식 통일**: "맞춤형 복지서비스 추천"(구 "추천 복지 서비스") 화면도 드롭다운 대신 이름 검색 위젯(`member-search.js`)으로 교체 — 이제 회원 선택이 필요한 화면 4곳(정보수정/삭제/서류출력/복지추천) 전부 같은 방식.
3. **메뉴명 변경**: "복지 추천" → "맞춤형 복지서비스 추천" (사이드바, 페이지 제목, 대시보드 바로가기 카드 전부 반영).
4. **통계 그래프**: 도넛(가운데 구멍) → 꽉 찬 원형(파이) 차트로 변경. 데이터라벨/범례 글자 크기를 11px→13px로 키움. 막대 차트(월별 신규가입 현황)는 `maxBarThickness`로 막대 최대 두께를 제한해서, 데이터가 1~2개월치만 있을 때 막대가 과도하게 커 보이지 않게 함(데이터가 늘어나면 Chart.js가 자동으로 막대 폭을 좁혀 맞추는 건 원래 동작 - 이번 제한은 "적을 때 너무 커 보이는" 반대 상황만 보완한 것).
5. **회색 보조텍스트 전부 굵게**: `text-muted` 색상(라벨, 카드 부제, 통계 상세 등)에 전부 `font-weight:600` 적용 + 색상 자체도 `#8A8378`→`#6B6357`로 더 진하게. 예전엔 이 클래스가 색만 정의하고 굵기가 없어서 흐릿하게 보였음.
6. **신규등록/수정 폼 레이아웃 정리**: 넓어진 화면 폭(1600px)에 폼을 그대로 늘어뜨려서 필드 위치가 들쭉날쭉해 보이던 문제 → 폼 전체를 `max-w-3xl`(약 864px)로 제한하고 가운데 정렬, 각 필드 그룹을 `flex-wrap` 대신 `grid`(고정 열)로 바꿔서 화면 폭과 상관없이 항상 같은 줄에 같은 칸으로 정렬되게 함.
7. **입력창 테두리 재확인**: `border` 규칙이 이제 확실히 일반 선택자(우선순위 있음)로 적용되고 있는지 다시 코드로 확인함.

**왜**: 사용자가 화면을 실제로 넓은 모니터에서 써보고 준 구체적 피드백 8건.

**검증**: curl로 로그인 후 전체 화면(대시보드/목록/등록/수정/삭제/서류출력/통계/추천) 200 렌더링 확인.

**참고**: "데이터가 늘어나면 막대그래프가 자동으로 작아지나요?"라는 질문 — 네, Chart.js는 카테고리(달) 개수에 맞춰 막대 폭을 자동으로 좁혀줍니다(내장 동작, 별도 설정 불필요). 지금 추가한 `maxBarThickness`는 반대 상황(데이터가 적어서 막대가 화면을 꽉 채울 만큼 두꺼워지는 것)을 막는 "최대 두께 제한"입니다.

## 2026-08-14 — 회원 선택 화면 4곳에 "목록에서 바로 선택" 기능 추가

**무엇을**: 정보수정/회원삭제/서류출력/맞춤형 복지서비스 추천 화면에서 지금까지는 이름을 검색해야만 회원이 나타났는데, 이제 화면에 들어가자마자 전체 회원이 카드 목록으로 바로 보이고 그 중 하나를 클릭해서 고를 수 있음(회원 목록 화면과 같은 느낌). 회원이 많아지는 경우를 대비해 이름 검색으로 좁혀보는 기능은 그대로 둠(입력하면 그 목록이 검색 결과로 필터링됨). 목록 영역은 세로 스크롤(최대 높이 420px)로 제한해서 회원이 아무리 많아져도 화면이 끝없이 길어지지 않게 함. 신규 등록 화면은 사용자가 "그대로 두는 게 최선인 것 같다"고 확정해서 이번엔 손대지 않음.

**왜**: 검색만 되던 게 아니라, 회원 목록처럼 눈으로 훑어보면서 고를 수도 있어야 편하다는 피드백.

**검증**: curl로 4개 화면 200 렌더링 재확인.

## 2026-08-14 — 통계 색상/라벨 다듬기, 디버깅 + 동시접속 테스트

**무엇을**:
1. **전체 화면 폰트/여백 일관성 재점검**: 통계 화면의 분포 차트 8개 제목만 다른 화면 제목보다 작은 크기(`text-sm`)로 남아있던 걸 찾아서 통일. 나머지(제목 크기, 카드 최대폭, 본문 캡션 크기 등)는 이미 지난 작업들에서 통일돼 있는 것을 재확인.
2. **통계 그래프 색상을 톤은 유지하되 한 단계 밝게**: 오렌지/그린 계열 색상을 각각 한 단계 밝은 색상(예: `#F97316`→`#FB923C`, `#22C55E`→`#4ADE80`)으로 교체.
3. **데이터 레이블(조각/막대 위 숫자) 색상**: 흰색 → 진한 잉크색(`#3A3A3A`)으로 변경 - 조각 색이 밝아지면서 흰 글씨는 대비가 약해질 수 있어서. 글자 굵기도 `bold`에서 `medium`(500) 정도로 낮춤.
4. **월별 신규가입 현황 막대에도 데이터 레이블**: 위와 동일한 색으로 통일 적용(이미 있었지만 색상 기준을 통계 전체와 맞춤).
5. **디버깅 테스트**: 서버 로그 전체를 다시 훑어 에러/트레이스백이 없는지 확인(기존에 있던 pandas 경고 1건 외엔 없음). 로그인→전체 화면 렌더링→회원 등록→서류생성→수정→AI추천 시작→삭제까지 curl로 재확인.
6. **동시접속 테스트**: 사용자 20명이 동시에 로그인해서 각자 9개 화면/API를 동시에 요청하는 상황을 흉내냄 → 총 180개 요청 전부 정상(0건 실패, 7.4초). 추가로 10명이 동시에 회원을 등록하는 상황도 테스트 → 9건 정상 등록, 1건은 우연히 예전 테스트 데이터와 전화번호가 겹쳐서 "중복 전화번호" 오류가 정상적으로 뜬 것(버그 아니라 의도된 검증 로직이 정확히 작동한 것). 경쟁 상태(race condition)로 인한 오류나 서버 다운은 없었음. 테스트로 만든 데이터는 전부 정리함.

**왜**: 사용자의 세부 디자인 피드백 + "이 정도 기능을 실제로 여러 명이 동시에 써도 괜찮은지" 확인 요청.

**검증**: 위 5, 6번 항목이 검증 그 자체입니다. 추가로 curl로 통계 화면 200 렌더링 재확인.

**참고 개념**:
- **동시접속 테스트(load test)**: 실제 서비스에서는 직원 여러 명이 동시에 접속할 수 있으니, 미리 여러 요청을 한꺼번에 보내보고 서버가 정상적으로 다 처리하는지 확인하는 게 중요합니다. 이번엔 파이썬으로 20개의 "가짜 사용자"를 동시에 만들어 각각 여러 화면을 요청하게 시켜봤습니다 - 이 정도 규모(직원 몇 명)에서는 지금 구조로 충분히 안정적이라는 걸 확인했습니다.

## 2026-08-14 — 회원 카드 통일(성별/나이/정보갱신 필요 표시), updated_at 컬럼 추가

**무엇을**:
1. **`updated_at` 컬럼 추가** (`sql/009_add_updated_at.sql`, 사용자가 Supabase SQL Editor에서 실행 완료): 회원 정보를 수정할 때마다 현재 시각으로 갱신되는 컬럼. 기존엔 `created_at`(최초 등록일)만 있어서 "언제 마지막으로 정보를 고쳤는지" 알 방법이 없었음.
2. **공용 회원 카드**(`static/js/member-card.js` 신규): 아바타, "이름 (성별 / 만나이)", 배지(회원구분/장애/정보갱신필요), 주소·전화·가입일을 만드는 로직을 한 곳에 모아서 회원 목록(`members-list.js`)과 회원 선택 위젯(`member-search.js`)이 완전히 같은 카드를 쓰게 함.
3. **이름 옆에 (성별 / 만나이)** 표시: 기존에 따로 있던 "성별" 배지는 제거하고, 이름 옆에 바로 보이도록 이동(중복 정보라 통합).
4. **"정보 갱신 필요" 배지**: `updated_at`(없으면 `created_at`) 기준으로 365일이 지난 회원 카드에 자동으로 뜸. 회원 정보를 수정하면 그 시점부터 다시 365일 카운트가 시작됨.
5. **회원수정/삭제/서류출력/맞춤형 복지서비스 추천** 4개 화면도 이제 회원 목록과 완전히 같은 카드 모양으로 통일.

**왜**: 사용자가 화면마다 회원을 다르게(카드/목록) 보여주는 게 아니라 일관되게 카드로 보이길 원했고, 나이/성별을 이름 옆에서 바로 확인하고 싶어했으며, 오래 갱신 안 된 회원 정보를 놓치지 않게 안내가 필요하다고 요청함.

**검증**: SQL 적용 후 `updated_at` 컬럼 정상 확인, 수정 시 `updated_at`이 실제로 갱신되는 것 확인, 나이/정보갱신필요 판정 로직을 파이썬으로 동일하게 재현해 6명 실 데이터로 교차 검증(전부 최근 등록이라 "정보갱신필요" 없음, 정상). 전체 화면 200 렌더링 재확인.

**⚠️ 작업 중 실수 발생 및 처리**: `updated_at` 갱신을 검증하는 curl 테스트를 만들다가, 수정 전 값을 미리 저장해두지 않고 회원 하나(id 52, "홍나무")의 성별/생년월일/주소/전화번호를 빈 값으로 덮어써버렸습니다. 사용자에게 바로 알리고 확인을 구한 결과 오늘 세션 중 만든 더미 테스트 데이터로 확인되어, 사용자 지시에 따라 그 회원을 삭제(소프트 삭제)해서 정리했습니다. **교훈**: 앞으로 실제 데이터를 대상으로 검증용 쓰기 작업을 할 때는, 수정 전 값을 반드시 먼저 백업(조회)해두고 진행해야 합니다.

**참고 개념**:
- **만 나이 계산**: 생년월일과 오늘 날짜를 연도 차이로 단순히 빼면 안 되고, "올해 생일이 지났는지"를 같이 봐야 정확합니다(예: 생일이 12월인데 지금이 8월이면 1살을 더 빼야 함). 이 로직을 파이썬(`stats.py`)과 자바스크립트(`member-card.js`) 양쪽에 똑같이 구현해뒀습니다.

## 2026-08-14 — 회원 선택 4개 화면 와이드 레이아웃 적용

**무엇을**: 정보수정/회원삭제/서류출력/맞춤형 복지서비스 추천 화면이 `max-w-lg`(약 576px)로 좁게 고정돼 있던 걸 제거해서, 회원 목록 화면처럼 화면 전체 폭을 사용하도록 변경. 검색창 자체는 여전히 420px로 좁게 유지(너무 넓으면 오히려 안 좋음), 그 아래 회원 카드 목록만 넓은 폭을 활용해 한 줄에 더 많은 카드가 보이게 됨. 회원 선택 후 나오는 "선택된 회원 상세 카드"(삭제/서류출력 화면)는 반대로 너무 넓게 늘어나 보이지 않도록 최대폭(`max-w-md`)을 따로 줌.

**왜**: 사용자 요청.

**검증**: curl로 4개 화면 200 렌더링 재확인.
